import { connect } from 'react-redux';
import WalletScreen from './view';

const mapStateToProps = () => ({
});

const mapDispatchToProps = dispatch => ({
});

export default connect(mapStateToProps, mapDispatchToProps)(WalletScreen);